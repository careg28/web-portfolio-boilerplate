import { AnimateInViewDirective } from './animate-in-view.directive';

describe('AnimateInViewDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateInViewDirective();
    expect(directive).toBeTruthy();
  });
});
